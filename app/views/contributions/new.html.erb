<!-- <div class="container">
  <%= simple_form_for [@recipient, @item, @contribution] do |f| %>
  <div class="col-xs-6 col-xs-offset-2">

    <%= f.input :amount_cents, label: "Amount in Pounds" %>
    <%= f.input :message %>
    <%#= f.submit id: "sub-button" %>
  <% end %>
  <div id = 'test' data-amount =<%= @myamount %>></div>
  </div>
</div>

 <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
    data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
    data-name="My Contribution"
    data-email="<%= current_user.email %>"
    data-description="Contribution <%= @contribution.item_sku %>"
    data-amount="#{myAmount}"
    data-currency="<%#= @contribution.amount.currency %>">

    </script>
<script>
  const stripeBtn = document.querySelector(".stripe-button-el");
  stripeBtn.innerHTML = "Pay Contribution"
  stripeBtn.addEventListener("click", ((e)=> {
    const amount = document.getElementById('contribution_amount_cents').value
      document.querySelector(".stripe-button").attr('data-amount', amount)
  }))
</script>

 -->
